// Code generated by protoc-gen-connect-go. DO NOT EDIT.
//
// Source: principal/v1/principal.proto

package principalv1connect

import (
	connect "connectrpc.com/connect"
	context "context"
	errors "errors"
	v1 "github.com/wolfeidau/airunner/api/gen/proto/go/principal/v1"
	http "net/http"
	strings "strings"
)

// This is a compile-time assertion to ensure that this generated file and the connect package are
// compatible. If you get a compiler error that this constant is not defined, this code was
// generated with a version of connect newer than the one compiled into your binary. You can fix the
// problem by either regenerating this code with an older version of connect or updating the connect
// version compiled into your binary.
const _ = connect.IsAtLeastVersion1_13_0

const (
	// CredentialServiceName is the fully-qualified name of the CredentialService service.
	CredentialServiceName = "principal.v1.CredentialService"
)

// These constants are the fully-qualified names of the RPCs defined in this package. They're
// exposed at runtime as Spec.Procedure and as the final two segments of the HTTP route.
//
// Note that these are different from the fully-qualified method names used by
// google.golang.org/protobuf/reflect/protoreflect. To convert from these constants to
// reflection-formatted method names, remove the leading slash and convert the remaining slash to a
// period.
const (
	// CredentialServiceImportCredentialProcedure is the fully-qualified name of the CredentialService's
	// ImportCredential RPC.
	CredentialServiceImportCredentialProcedure = "/principal.v1.CredentialService/ImportCredential"
	// CredentialServiceListCredentialsProcedure is the fully-qualified name of the CredentialService's
	// ListCredentials RPC.
	CredentialServiceListCredentialsProcedure = "/principal.v1.CredentialService/ListCredentials"
	// CredentialServiceRevokeCredentialProcedure is the fully-qualified name of the CredentialService's
	// RevokeCredential RPC.
	CredentialServiceRevokeCredentialProcedure = "/principal.v1.CredentialService/RevokeCredential"
)

// CredentialServiceClient is a client for the principal.v1.CredentialService service.
type CredentialServiceClient interface {
	// ImportCredential imports a worker credential from a PEM-encoded public key.
	// Creates a new worker principal and stores the public key.
	// Requires admin role.
	ImportCredential(context.Context, *connect.Request[v1.ImportCredentialRequest]) (*connect.Response[v1.ImportCredentialResponse], error)
	// ListCredentials returns all credentials (principals) for the current user's org.
	// This RPC is idempotent and cacheable - uses HTTP GET for better caching.
	ListCredentials(context.Context, *connect.Request[v1.ListCredentialsRequest]) (*connect.Response[v1.ListCredentialsResponse], error)
	// RevokeCredential revokes a credential by principal ID.
	// Soft-deletes the principal and adds fingerprint to revocation list.
	// Requires admin role.
	RevokeCredential(context.Context, *connect.Request[v1.RevokeCredentialRequest]) (*connect.Response[v1.RevokeCredentialResponse], error)
}

// NewCredentialServiceClient constructs a client for the principal.v1.CredentialService service. By
// default, it uses the Connect protocol with the binary Protobuf Codec, asks for gzipped responses,
// and sends uncompressed requests. To use the gRPC or gRPC-Web protocols, supply the
// connect.WithGRPC() or connect.WithGRPCWeb() options.
//
// The URL supplied here should be the base URL for the Connect or gRPC server (for example,
// http://api.acme.com or https://acme.com/grpc).
func NewCredentialServiceClient(httpClient connect.HTTPClient, baseURL string, opts ...connect.ClientOption) CredentialServiceClient {
	baseURL = strings.TrimRight(baseURL, "/")
	credentialServiceMethods := v1.File_principal_v1_principal_proto.Services().ByName("CredentialService").Methods()
	return &credentialServiceClient{
		importCredential: connect.NewClient[v1.ImportCredentialRequest, v1.ImportCredentialResponse](
			httpClient,
			baseURL+CredentialServiceImportCredentialProcedure,
			connect.WithSchema(credentialServiceMethods.ByName("ImportCredential")),
			connect.WithClientOptions(opts...),
		),
		listCredentials: connect.NewClient[v1.ListCredentialsRequest, v1.ListCredentialsResponse](
			httpClient,
			baseURL+CredentialServiceListCredentialsProcedure,
			connect.WithSchema(credentialServiceMethods.ByName("ListCredentials")),
			connect.WithIdempotency(connect.IdempotencyNoSideEffects),
			connect.WithClientOptions(opts...),
		),
		revokeCredential: connect.NewClient[v1.RevokeCredentialRequest, v1.RevokeCredentialResponse](
			httpClient,
			baseURL+CredentialServiceRevokeCredentialProcedure,
			connect.WithSchema(credentialServiceMethods.ByName("RevokeCredential")),
			connect.WithClientOptions(opts...),
		),
	}
}

// credentialServiceClient implements CredentialServiceClient.
type credentialServiceClient struct {
	importCredential *connect.Client[v1.ImportCredentialRequest, v1.ImportCredentialResponse]
	listCredentials  *connect.Client[v1.ListCredentialsRequest, v1.ListCredentialsResponse]
	revokeCredential *connect.Client[v1.RevokeCredentialRequest, v1.RevokeCredentialResponse]
}

// ImportCredential calls principal.v1.CredentialService.ImportCredential.
func (c *credentialServiceClient) ImportCredential(ctx context.Context, req *connect.Request[v1.ImportCredentialRequest]) (*connect.Response[v1.ImportCredentialResponse], error) {
	return c.importCredential.CallUnary(ctx, req)
}

// ListCredentials calls principal.v1.CredentialService.ListCredentials.
func (c *credentialServiceClient) ListCredentials(ctx context.Context, req *connect.Request[v1.ListCredentialsRequest]) (*connect.Response[v1.ListCredentialsResponse], error) {
	return c.listCredentials.CallUnary(ctx, req)
}

// RevokeCredential calls principal.v1.CredentialService.RevokeCredential.
func (c *credentialServiceClient) RevokeCredential(ctx context.Context, req *connect.Request[v1.RevokeCredentialRequest]) (*connect.Response[v1.RevokeCredentialResponse], error) {
	return c.revokeCredential.CallUnary(ctx, req)
}

// CredentialServiceHandler is an implementation of the principal.v1.CredentialService service.
type CredentialServiceHandler interface {
	// ImportCredential imports a worker credential from a PEM-encoded public key.
	// Creates a new worker principal and stores the public key.
	// Requires admin role.
	ImportCredential(context.Context, *connect.Request[v1.ImportCredentialRequest]) (*connect.Response[v1.ImportCredentialResponse], error)
	// ListCredentials returns all credentials (principals) for the current user's org.
	// This RPC is idempotent and cacheable - uses HTTP GET for better caching.
	ListCredentials(context.Context, *connect.Request[v1.ListCredentialsRequest]) (*connect.Response[v1.ListCredentialsResponse], error)
	// RevokeCredential revokes a credential by principal ID.
	// Soft-deletes the principal and adds fingerprint to revocation list.
	// Requires admin role.
	RevokeCredential(context.Context, *connect.Request[v1.RevokeCredentialRequest]) (*connect.Response[v1.RevokeCredentialResponse], error)
}

// NewCredentialServiceHandler builds an HTTP handler from the service implementation. It returns
// the path on which to mount the handler and the handler itself.
//
// By default, handlers support the Connect, gRPC, and gRPC-Web protocols with the binary Protobuf
// and JSON codecs. They also support gzip compression.
func NewCredentialServiceHandler(svc CredentialServiceHandler, opts ...connect.HandlerOption) (string, http.Handler) {
	credentialServiceMethods := v1.File_principal_v1_principal_proto.Services().ByName("CredentialService").Methods()
	credentialServiceImportCredentialHandler := connect.NewUnaryHandler(
		CredentialServiceImportCredentialProcedure,
		svc.ImportCredential,
		connect.WithSchema(credentialServiceMethods.ByName("ImportCredential")),
		connect.WithHandlerOptions(opts...),
	)
	credentialServiceListCredentialsHandler := connect.NewUnaryHandler(
		CredentialServiceListCredentialsProcedure,
		svc.ListCredentials,
		connect.WithSchema(credentialServiceMethods.ByName("ListCredentials")),
		connect.WithIdempotency(connect.IdempotencyNoSideEffects),
		connect.WithHandlerOptions(opts...),
	)
	credentialServiceRevokeCredentialHandler := connect.NewUnaryHandler(
		CredentialServiceRevokeCredentialProcedure,
		svc.RevokeCredential,
		connect.WithSchema(credentialServiceMethods.ByName("RevokeCredential")),
		connect.WithHandlerOptions(opts...),
	)
	return "/principal.v1.CredentialService/", http.HandlerFunc(func(w http.ResponseWriter, r *http.Request) {
		switch r.URL.Path {
		case CredentialServiceImportCredentialProcedure:
			credentialServiceImportCredentialHandler.ServeHTTP(w, r)
		case CredentialServiceListCredentialsProcedure:
			credentialServiceListCredentialsHandler.ServeHTTP(w, r)
		case CredentialServiceRevokeCredentialProcedure:
			credentialServiceRevokeCredentialHandler.ServeHTTP(w, r)
		default:
			http.NotFound(w, r)
		}
	})
}

// UnimplementedCredentialServiceHandler returns CodeUnimplemented from all methods.
type UnimplementedCredentialServiceHandler struct{}

func (UnimplementedCredentialServiceHandler) ImportCredential(context.Context, *connect.Request[v1.ImportCredentialRequest]) (*connect.Response[v1.ImportCredentialResponse], error) {
	return nil, connect.NewError(connect.CodeUnimplemented, errors.New("principal.v1.CredentialService.ImportCredential is not implemented"))
}

func (UnimplementedCredentialServiceHandler) ListCredentials(context.Context, *connect.Request[v1.ListCredentialsRequest]) (*connect.Response[v1.ListCredentialsResponse], error) {
	return nil, connect.NewError(connect.CodeUnimplemented, errors.New("principal.v1.CredentialService.ListCredentials is not implemented"))
}

func (UnimplementedCredentialServiceHandler) RevokeCredential(context.Context, *connect.Request[v1.RevokeCredentialRequest]) (*connect.Response[v1.RevokeCredentialResponse], error) {
	return nil, connect.NewError(connect.CodeUnimplemented, errors.New("principal.v1.CredentialService.RevokeCredential is not implemented"))
}

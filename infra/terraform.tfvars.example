# Copy this file to terraform.tfvars and update with your values

aws_region        = "us-east-1"
domain_name       = "example.com"

# Additional tags (optional)
tags = {
  Project   = "github.com/wolfeidau/airunner"
  ManagedBy = "terraform"
}
